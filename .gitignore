# macOS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes

# Claude Code
.claude/

# Node / Vite / React Router build artifacts — GitHub Actions rebuilds these,
# they are never committed.
node_modules/
build/
.react-router/

# Secrets — .env is a personal, uncommitted notes file (see .env's own header)
.env
.env.local
.env.*.local
